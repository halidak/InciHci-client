import React from "react";
import { Text } from "react-native";

export const ProductComponents = ({ categoryId }) => {
  return (
    <Text>Product Screen for Category ID: {categoryId}</Text>
  );
};
